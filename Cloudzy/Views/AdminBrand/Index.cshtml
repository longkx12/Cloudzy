@{
    ViewData["Title"] = "Quản lý nhãn hàng";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h2>Danh sách nhãn hàng</h2>
<a class="btn btn-primary mb-3" asp-action="Create">Thêm nhãn hàng</a>

<!-- Danh sách nhãn hàng -->
<div id="brandList">
    <p>Đang tải dữ liệu...</p>
</div>

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script>
    $(document).ready(function () {
        load(1);

        var message = '@Html.Raw(TempData["SuccessMessage"])';
        if (message) {
            showAlert(message);
        }
    });

    function load(page) {
        $.ajax({
            url: '/AdminBrand/Load?page=' + page,
            type: 'GET',
            success: function (data) {
                $('#brandList').html(data);
            },
            error: function () {
                $('#brandList').html('<p class="text-danger">Không thể tải dữ liệu.</p>');
            }
        });
    }
</script>
